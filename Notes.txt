Notes on process:

1) Extract gadgets, and gadgets of certain kinds (e.g. write what - where)
Where to start what we define to be a gadget?
Might go over the binaries in multiple passes looking for gadgets of particular behavior?

Do the following with the extracted gadgets:

2) Find some writable addresses that are executable, or change permissions of an existing page to be executable
Suggestion: Call mmap (system call 9) to create a page with executable permissions
(Requires being able to modify rdi, rsi, rdx, r10, r8, r9)

3) Write our secondary shellcode to the memory

4) Transfer control to our secondary shellcode
